# search for images with name ubuntu
docker search ubuntu
docker search ubuntu > docker.search.ubuntu.txt
vi docker.search.ubuntu.txt
docker search bionic
# get the image locally, for use in my Dockerfile
docker pull ubuntu
# show the images
docker images

